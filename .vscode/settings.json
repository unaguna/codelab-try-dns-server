{
    "runOnSave.commands": [
        // export codelab on save
        {
            "globMatch": "src/**/*.md",
            "command": "claat export -o /workspace_local/dist ${file}",
            "runIn": "backend"
        }
    ]
}