{
    "runOnSave.commands": [
        // export codelab on save
        {
            "globMatch": "src/**/*.md",
            "command": "claat export -o ${env:DIST_DIR} ${file}",
            "runIn": "backend"
        }
    ]
}